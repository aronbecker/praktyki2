Najpierw trzeba zainstalować te 2 zależności
- `pip install openpyxl`
- `pip install pandas`

Należy terminal w lokalizacji root (/) projektu wykonać

`py -m import.main database_url` (np. mysql://root:@localhost/)praktyki
